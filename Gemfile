# ################################################################################
# Author(s):    Sebastian Gerau
#
# Description:  Bundler 'Gemfile' for ruby gem management.
#
# NOTICE:       The listed tag versions are current as of the time this 'Gemfile' 
#               was created / committed. Be sure to check the repositories for newer 
#               versions if you want guaranteed to be using the latest releases, 
#               and adapt this file accordingly.
#               For using a gems latest development state (NOT RECOMMENDED) simply 
#               comment out the 'tag' parameter in front of the leading comma.
# ################################################################################
source 'https://rubygems.org'

# Recommended for general use.
#gem 'berkshelf', :git => 'git://github.com/berkshelf/berkshelf.git', :tag => 'v3.1.4'
gem 'berkshelf'

# Only required if directly working with Chef Server
#gem 'knife-solo', :git => 'git://github.com/matschaffer/knife-solo.git', :tag => 'v0.4.2'

# Only use if testing/working with non-official Vagrant and/or Vagrant Plugin versions
#gem 'vagrant', :git => 'git://github.com/mitchellh/vagrant.git', :tag => 'v1.6.3'

group :vagrant_plugins do
  #gem 'vagrant-vbguest', :git => 'git://github.com/dotless-de/vagrant-vbguest.git', :tag => 'v0.10.0'
  #gem 'vagrant-omnibus', :git => 'git://github.com/schisamo/vagrant-omnibus.git', :tag => 'v1.4.1'
  #gem 'vagrant-berkshelf', :git => 'git://github.com/berkshelf/vagrant-berkshelf.git' #, :tag => 'v2.0.1'
  #gem 'vagrant-cachier', :git => 'git://github.com/fgrehm/vagrant-cachier.git', :tag => 'v0.8.0'
end
