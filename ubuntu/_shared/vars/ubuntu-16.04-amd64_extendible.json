{
  "_comment": "Variables for building Ubuntu Desktop or Server 16.04 LTS 64-bit images which are to be further extended, e.g. with pre-installed software or configurations.",

  "_comment": "Pre-requisite is `ubuntu-16.04-amd64.json` having been built successfully, which creates and persists the OVF file used by this build.",

  "_comment": "Validation Command:  `packer validate -var-file='../_shared/vars/ubuntu-16.04-amd64_extendible.json' -var 'ovf_source_file=<name_of_ovf_file_to_use>.ovf' -var 'image_id=<unique_image_id>' -only='vb_plain_{image_id}' ubuntu.json`",

  "_comment": "Build Command:       `packer build -var-file='../_shared/vars/ubuntu-16.04-amd64_extendible.json' -var 'ovf_source_file=<name_of_ovf_file_to_use>.ovf' -var 'image_id=<unique_image_id>' -only='vb_plain_{image_id}' ubuntu.json`",

  "box_basename": "ubuntu-16.04",
  "template": "ubuntu-16.04-amd64",

  "scripts": ""
}
