{
  "_comment": "Build definition for Ubuntu Desktop or Server 16.04 LTS 64-bit to be further extended, e.g. with pre-installed software or configurations.",

  "_comment": "Pre-requisite is `ubuntu-16.04-amd64.json` having been built successfully, which creates and persists the OVF file used by this build.",

  "_comment": "Validation Command:  `packer validate -var-file='../_shared/vars/ubuntu-16.04-amd64_extendible.json' -var 'ovf_source_file=<name_of_ovf_file_to_use>.ovf' -var 'artifact_id=<unique_artifact_id>' -only='vb_plain_{artifact_id}' ubuntu.json`",

  "_comment": "Build Command:       `packer build -var-file='../_shared/vars/ubuntu-16.04-amd64_extendible.json' -var 'ovf_source_file=<name_of_ovf_file_to_use>.ovf' -var 'artifact_id=<unique_artifact_id>' -only='vb_plain_{artifact_id}' ubuntu.json`",

  "box_basename": "ubuntu-16.04",
  "template": "ubuntu-16.04-amd64"
}
